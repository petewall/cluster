apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - kubeai-helm-release.yaml
  # models.example.yaml is intentionally NOT listed — Model CRs are workloads.
  # See that file for how to enable one.
