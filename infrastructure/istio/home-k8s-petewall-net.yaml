---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: home-k8s-petewall-net-tls
  namespace: istio-system
spec:
  secretName: home-k8s-petewall-net-tls
  dnsNames:
    - home-k8s.petewall.net
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
---
apiVersion: networking.istio.io/v1
kind: Gateway
metadata:
  name: home-k8s-petewall-net-gateway
  namespace: istio-system
spec:
  selector:
    istio: ingressgateway
  servers:
    - port:
        number: 443
        name: https
        protocol: HTTPS
      hosts:
        - home-k8s.petewall.net
      tls:
        mode: SIMPLE
        credentialName: home-k8s-petewall-net-tls
    - port:
        number: 80
        name: http
        protocol: HTTP
      hosts:
        - home-k8s.petewall.net
      tls:
        httpsRedirect: true
