---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: recipes-petewall-net-tls
  namespace: istio-system
spec:
  secretName: recipes-petewall-net-tls
  dnsNames:
    - recipes.petewall.net
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
---
apiVersion: networking.istio.io/v1
kind: Gateway
metadata:
  name: recipes-petewall-net-gateway
  namespace: istio-system
spec:
  selector:
    istio: ingressgateway
  servers:
    - port:
        number: 443
        name: https
        protocol: HTTPS
      hosts:
        - recipes.petewall.net
      tls:
        mode: SIMPLE
        credentialName: recipes-petewall-net-tls
    - port:
        number: 80
        name: http
        protocol: HTTP
      hosts:
        - recipes.petewall.net
      tls:
        httpsRedirect: true
