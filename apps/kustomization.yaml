apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - monitoring
  - petewall-net
  # ironwall is managed by its own Flux Kustomization
  # (cluster/ironwall.yaml) so it can be suspended independently.
