apiVersion: apps/v1
kind: Deployment
metadata:
  name: synthetic-monitoring-agent
  namespace: monitoring
  annotations:
    ignore-check.kube-linter.io/drop-net-raw-capability: NET_RAW required for ICMP ping/traceroute probes
spec:
  minReadySeconds: 10
  replicas: 1
  revisionHistoryLimit: 10
  selector:
    matchLabels:
      name: synthetic-monitoring-agent
  strategy:
    rollingUpdate:
      maxSurge: 0
      maxUnavailable: 1
  template:
    metadata:
      labels:
        name: synthetic-monitoring-agent
    spec:
      containers:
      - args:
        - --api-server-address=$(API_SERVER)
        - --api-token=$(API_TOKEN)
        - --verbose=true
        env:
        - name: API_TOKEN
          valueFrom:
            secretKeyRef:
              key: api-token
              name: synthetic-monitoring-api
        - name: API_SERVER
          valueFrom:
            secretKeyRef:
              key: api-server
              name: synthetic-monitoring-api
        image: grafana/synthetic-monitoring-agent:v0.60.0
        name: agent
        ports:
        - containerPort: 4050
          name: http-metrics
        securityContext:
          capabilities:
            add:
            - NET_RAW
            drop:
            - all
          readOnlyRootFilesystem: true
          runAsNonRoot: true
          runAsUser: 12345
        resources:
          requests:
            cpu: 10m
            memory: 32Mi
          limits:
            memory: 64Mi
        volumeMounts:
        - name: tmp
          subPath: tmp
          mountPath: /tmp
      volumes:
      - name: tmp
        emptyDir: {}
